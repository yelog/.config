return {
  "sphamba/smear-cursor.nvim",
  opts = {
    cursor_color = "#18B06A",
    -- stiffness = 0.5,
    -- trailing_stiffness = 0.3,
    -- trailing_exponent = 3,
    -- gamma = 1,
    -- volume_reduction_exponent = -0.1,
  }
}

